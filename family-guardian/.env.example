# =============================================================================
# family-guardian environment configuration
# =============================================================================
# Copy this file to `.env` and fill in real values.
#   cp .env.example .env

# -----------------------------------------------------------------------------
# Run mode
# -----------------------------------------------------------------------------
# "elder" : 노부모 낙상 감지 모드 (사고 시에만 영상 보존)
# "baby"  : 영유아 호흡 모니터링 + 상시 녹화 모드
MODE=elder

# -----------------------------------------------------------------------------
# Camera
# -----------------------------------------------------------------------------
# macOS / Windows 노트북 내장 카메라는 보통 0번
CAMERA_INDEX=0

# -----------------------------------------------------------------------------
# Notifier
# -----------------------------------------------------------------------------
# "console" 또는 "telegram"
NOTIFIER=console

# -----------------------------------------------------------------------------
# Telegram bot setup
# -----------------------------------------------------------------------------
# 1) 텔레그램에서 @BotFather 검색 → /newbot 으로 봇 생성 → 토큰 발급
# 2) 만든 봇과 1:1 대화방을 열고 /start 메시지 전송
# 3) 브라우저에서 아래 URL 열기 (YOUR_TOKEN 부분 치환):
#    https://api.telegram.org/botYOUR_TOKEN/getUpdates
# 4) 응답 JSON의 "chat":{"id": ... } 값을 TELEGRAM_CHAT_ID 에 입력
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# 알림 영상을 텔레그램에 첨부할지 (50MB 제한)
TELEGRAM_ATTACH_VIDEO=true

# -----------------------------------------------------------------------------
# Cloud upload (선택)
# -----------------------------------------------------------------------------
CLOUD_UPLOAD_ENABLED=false
AWS_REGION=ap-northeast-2
S3_BUCKET=

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
